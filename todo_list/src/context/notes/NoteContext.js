// this folder is just being used to initialize context to be called in 
// NoteState. It can also be done in notestate

import { createContext } from "react";

const noteContext = createContext();

export default noteContext;